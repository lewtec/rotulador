package layout

templ Document(title string) {
	<!DOCTYPE html>
	<html lang="en" data-theme="light" class="app-frame">
		<head>
			<meta charset="UTF-8"/>
			// viewport-fit=cover so env(safe-area-inset-*) works; app-frame uses dvh when supported.
			<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover"/>
			if title != "" {
				<title>{ title }</title>
			} else {
				<title>Rotulador</title>
			}
			<link rel="icon" href="/favicon.svg" type="image/svg+xml"/>
			<script
				src="https://unpkg.com/htmx.org@1.9.6"
				integrity="sha384-FhXw7b6AlE/jyjlZH5iHa/tTe9EpJ1Y55RjcgPbjeWMskSxZt1v9qkxLJWNJaGni"
				crossorigin="anonymous"
			></script>
			<link rel="stylesheet" href="/static/style.css" type="text/css"/>
			<script>
				function reportError(error, context) {
					console.error("Error detected:", context, error);
				}
				function initTheme() {
					try {
						const theme = localStorage.getItem('theme') || 'light';
						document.documentElement.setAttribute('data-theme', theme);
					} catch (e) {
						reportError(e, "Failed to initialize theme from localStorage");
					}
				}
				function toggleTheme() {
					try {
						const html = document.documentElement;
						const currentTheme = html.getAttribute('data-theme');
						const newTheme = currentTheme === 'light' ? 'dark' : 'light';
						html.setAttribute('data-theme', newTheme);
						localStorage.setItem('theme', newTheme);
					} catch (e) {
						reportError(e, "Failed to toggle theme");
					}
				}
				initTheme();
			</script>
		</head>
		{ children... }
	</html>
}
